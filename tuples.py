#tuples are like lists, but tuples are immutable
#By immutable i  mean there values cannot be changed one they have been created

tuple_a = ("a","b","c","d") # tuple of strings
tuple_b = (1,2,3,4,5,6) # tuple of numbers
tuple_c = (1,"west",34,"longitude") # mixed tuple
tuple_d = tuple() #empty tuple

print(tuple_d)